{
  "application": {
    "name": "swe",
    "implementation": "rlm-0.1.3-upstream"
  },
  "limits": {
    "max_model_calls": 1,
    "max_concurrency": 1,
    "wall_time_seconds": 1800,
    "max_depth": 1
  },
  "capture_content": false,
  "repeats": 1,
  "random_seed": 20260810,
  "metadata": {
    "fidelity": "upstream_runtime_adapter",
    "version": "0.1.3",
    "revision": "72d6940142ddfb84ee6be573dc999a37e633e671",
    "boundary": "Runs the clean pinned official RLM runtime with its prompts and REPL through bounded JSON stdin. It injects no Scaffold Lab domain tools and makes no SWE patch-parity claim.",
    "accounting": "The v0.1.3 root UsageSummary does not merge recursive child handlers, so calls, tokens, and optional cost are retained only as lower bounds."
  }
}
