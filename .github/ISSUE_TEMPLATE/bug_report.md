---
name: Bug report
about: Something behaves differently from what the docs claim
labels: bug
---

**What happened**

**What you expected**

**Reproduction**

```bash
# the exact command (redact credentials; --provider-header values are fine to omit)
```

**Environment**

- mini-agent version (`mini-agent --version`):
- Python version:
- OS:
- Provider/protocol (e.g. `openai/...` Responses, `--protocol chat-completions`, `anthropic/...`):

**Evidence**

Attach the relevant `trace.jsonl` excerpt or `manifest.json` if the issue is
about a run or evaluation. Traces are secret-redacted by default; skim before
posting anyway.
