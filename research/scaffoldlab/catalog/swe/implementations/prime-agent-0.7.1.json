{
  "application": {
    "name": "swe",
    "implementation": "prime-agent-0.7.1"
  },
  "limits": {
    "max_model_calls": 1,
    "max_concurrency": 1,
    "wall_time_seconds": 1800,
    "max_depth": 1
  },
  "capture_content": false,
  "repeats": 1,
  "random_seed": 20260810,
  "metadata": {
    "fidelity": "exact_public_protocol",
    "version": "0.7.1",
    "boundary": "Exact at Prime Agent 0.7.1's documented JSON v3 session/event/agent_end CLI boundary. Every call uses fresh --no-session state; executable/package identity is proven only when its optional SHA pin is supplied, and runtime internals, daemon/schedule/continual state, complete child-tree usage, and security sandboxing remain unavailable.",
    "workspace_owner": "Prime Agent owns a caller-supplied disposable single-trial worktree; do not add a local environment block."
  }
}
