{
  "harnesses": [{"name": "prime_agent"}],
  "limits": {
    "max_model_calls": 1,
    "max_concurrency": 1,
    "wall_time_seconds": 1800,
    "max_depth": 4
  },
  "capture_content": false,
  "repeats": 1,
  "random_seed": 20260809,
  "metadata": {
    "accounting_scope": "observed root usage is a lower bound; whole-tree usage is unverified",
    "limits_scope": "max_model_calls and max_concurrency describe the one outer Scaffold Lab session; max_depth does not constrain the upstream Prime Agent tree"
  }
}
