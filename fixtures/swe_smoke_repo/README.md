# Fixture Repository

This is a provider-neutral experiment runtime fixture used only by mini-agent's
offline configuration and tool-loop smoke tests.

It contains no benchmark answer manifest, prior run artifacts, credentials, or
mutable state shared with the mini-agent source repository.
